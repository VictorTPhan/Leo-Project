using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;
public class ExpectedCosts : MonoBehaviour
{
    public TextMeshProUGUI loanQuaterlyCosts;
   

    // Update is called once per frame
    void Update()
    {
        //loanQuaterlyCosts.text = string.Format("Expected Losses : -$" + );
    }
}